html
    head
        title="Productos en Pug"
    body
        div 
            if productos.length>0
                each producto in productos
                    h2=producto.title
                    p=producto.price
                    img(width=100 height=100 src=producto.thumbnail)
                    br
                a(href="/" type="button")='Nuevo Producto'
            else 
                h2="No hay productos"